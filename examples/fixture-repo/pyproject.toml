[project]
name = "fixture-service"
version = "0.1.0"
description = "Synthetic fixture for langgraph-maintenance public demos."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
