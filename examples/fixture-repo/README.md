# Fixture Service

Synthetic repository used by the public demo config. It is intentionally small
and contains only safe placeholder code and documentation.

## Checks

- Source files live in `src/`.
- Tests live in `tests/`.
- One TODO marker exists so static search has useful output.
